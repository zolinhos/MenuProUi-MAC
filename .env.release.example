APP_NAME="MenuProUI-MAC"
APP_BUNDLE_ID="com.menuproui.mac"
APP_PATH="dist/MenuProUI-MAC.app"
DIST_DIR="dist"
ARCH_LABEL="macos-arm64"
MIN_MACOS="13.0"
DEFAULT_VERSION="1.7"

# Certificado Developer ID Application
DEV_ID_APP_CERT="Developer ID Application: Seu Nome (TEAMID)"

# Opção 1: Perfil salvo no keychain (recomendado)
NOTARY_PROFILE="notary-profile"

# Opção 2: credenciais diretas (use somente se não usar NOTARY_PROFILE)
AC_USERNAME=""
AC_PASSWORD=""
AC_TEAM_ID=""